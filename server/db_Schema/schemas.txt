CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  first_name TEXT NOT NULL,
  last_name TEXT NOT NULL,
  email TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL
);

CREATE TABLE accounts (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE
);

CREATE TABLE categories (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT CHECK (type IN ('income', 'expense')) NOT NULL,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE
);

CREATE TABLE transactions (
  id SERIAL PRIMARY KEY,
  amount DECIMAL(10,2) NOT NULL,
  description TEXT,
  account_id INTEGER NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
  category_id INTEGER REFERENCES categories(id) ON DELETE SET NULL,
  transaction_date DATE NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


THE FLOW OF THE TABLE IS: 

 users
 ├── accounts
 │     └── transactions
 │
 └── categories
        ↑
   transactions
   
meaning: transactions connects:
account ✅
category ✅
👉 NOT a chain — it's a graph



--------------------  TABLE EXAMPLES  --------------------
1. users table
id  first_name   last_name  email            password   
1   Sana         Matusala   bxbd@gmail.com   12345678
2   David        Said       david@ncc.com    87654321

2. accounts table
id     name     user_id (it's like, who's account is this?)
1      Savings  1 (sana's)
2      Main     2 (david's)
3      Cash     1 (sana's)
4      Cash     2 (david's)
5      Main     1 (Sana's)
6      Savings  2 (David's)

3. categories table 
id      name        type          user_id
1       Friend      income           1
2       Mom         income           1
3       Salary      income           2
4       Shopping    expense          2
5       Utilities   expense          1
6       Food        expense          1


4. transactions table 
id   amount   desc.n    acnt_id   cat.id  t.date      created_at
1    3000     romeo     5        1       01/01/2026  02/03/2026
2    150      kolo      5        6       02/03/2026  03/01/2030
3    700      t-shirt   6        4       11/03/2025  11/04/2026

👉 Every transaction must satisfy:
account.user_id === category.user_id

🧠 Server side must convert:
"Main" → account_id
"Shopping" → category_id

--------------------  ADD TRANSACTION FORM  --------------------

Amount: 150
Description: dabokolo
Account: Main
Category: Shopping
Date: 12/12/2026

income example
3000
my friend from Dubai 
main
income (Friend)


--------------------  API ENDPOINTS  --------------------
GET ALL
GET /api/transactions?user_id=1
GET /api/transactions?user_id=1&type=expense

GET ONE 
GET /api/transactions/1 

UPDATE
PUT /api/transactions/1

DELETE 
DELETE /api/transactions/1